import createimage

def main(model_name, bucket_name, output_path):
    # create image
    createimage.main(model_name, bucket_name, output_path)
    # resize image
    # upscale image
    # upload image to s3
